[package]
name = "test-with-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
test-with = { path = "../lib", features = ["runtime", "net", "resource", "user", "executable", "timezone"] }
tokio = "1.45.1"
